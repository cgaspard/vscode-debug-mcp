.vscode/**
.vscode-test/**
.github/**
.claude/**
src/**
sample-workspace/**
releasenotes/**
scripts/**
.gitignore
.eslintrc.json
.mcp.json
**/tsconfig.json
**/*.map
**/*.ts
# We bundle with esbuild — out/extension.js is the only runtime artifact we
# need from this repo. node_modules is excluded entirely because everything
# resolvable is already inlined into the bundle.
node_modules/**
package-lock.json
*.vsix
.DS_Store
